<div class="row">
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table" id="table_role">
                <thead>
                <tr>
                    <th>
                        {{ trans('message.attributes.name') }}
                    </th>
                    <th>
                        {{ trans('message.attributes.display_name') }}
                    </th>
                    <th>
                        {{ trans('message.attributes.permission') }}
                    </th>
                    <th>
                        {{ trans('message.attributes.action') }}
                    </th>
                </tr>
                </thead>
                <tbody></tbody>
            </table>
        </div>
    </div>
</div>